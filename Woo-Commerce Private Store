<?php
add_action( 'template_redirect', function (){
if( ( is_shop() || is_product() || is_product_category() || is_page( 'cart' ) || is_page( 'checkout' )) && !is_user_logged_in() ) {  
        wp_redirect( site_url( '/' ) );
        exit();
	}
} );
